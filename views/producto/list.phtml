<?php include 'views/templates/header.phtml'; ?>

<h1>Lista de Productos</h1>

<?php if (isset($_SESSION['user_id'])): ?>
    <a href="<?= BASE_URL ?>/productos/add" class="btn">Agregar Producto</a>
<?php endif; ?>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Cantidad</th>
            <th>Color</th>
            <th>Almacén</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($productos as $producto): ?>
            <tr>
                <td><?= $producto['id_producto'] ?></td>
                <td><?= htmlspecialchars($producto['nombre_producto']) ?></td>
                <td><?= $producto['cantidad_producto'] ?></td>
                <td><?= htmlspecialchars($producto['color_producto']) ?></td>
                <td><?= htmlspecialchars($producto['nombre_almacen']) ?></td>
                <td>
                    <a href="<?= BASE_URL ?>/productos/detail/<?= $producto['id_producto'] ?>">Ver</a>
                    <?php if (isset($_SESSION['user_id'])): ?>
                        <a href="<?= BASE_URL ?>/productos/edit/<?= $producto['id_producto'] ?>">Editar</a>
                        <a href="<?= BASE_URL ?>/productos/delete/<?= $producto['id_producto'] ?>" onclick="return confirm('¿Está seguro de eliminar este producto?')">Eliminar</a>
                    <?php endif; ?>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<?php include 'views/templates/footer.phtml'; ?>